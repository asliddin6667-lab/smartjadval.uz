# smartjadval split files

App.jsx katta fayldan modullarga bo'lindi.

## Ishlatish
1. Zipni oching.
2. Ichidagi `src` papkani loyihangizdagi `src` bilan almashtiring yoki fayllarni ustiga tashlang.
3. Terminalda: `npm run dev`

Agar PowerShell `npm.ps1` xato bersa:
`npm.cmd run dev`
